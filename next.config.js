/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'eu-west-2.graphassets.com', 'assets.aceternity.com'],
  },
};

module.exports = nextConfig;
